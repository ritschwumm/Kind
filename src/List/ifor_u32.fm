// Folds over a list, left associative
List.ifor_u32<A: Type>(xs: List(A))<B: Type>(b: B, f: U32 -> A -> B -> B): B
  case xs {
    nil : b,
    cons: List.ifor_u32<A>(xs.tail)<B>(f(0u,xs.head,b), (n) f(U32.add(n,1u)))
  }