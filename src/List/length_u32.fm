// Computes the length of the list.
List.length_u32<A: Type>(xs: List(A)): U32
  List.length_u32_go<_>(xs, 0u)

// Computes the length of the list.
List.length_u32_go<A: Type>(xs: List(A), n: U32): U32
  case xs{
    nil: n
    cons: List.length_u32_go<A>(xs.tail, U32.add(1u, n))
  }