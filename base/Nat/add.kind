// Adds two natural numbers (a, b)
// Can be written as 2 + 3 (Syntax sugar)
// - Nat.add(2, 3) == 5
// - Nat.add(0, 5) == 0

Nat.add(a: Nat, b: Nat): Nat
  case a {
    zero: b
    succ: Nat.succ(Nat.add(a.pred, b))
  }
