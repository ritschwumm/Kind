// Divides a natural number (a) by 2
// The remainder is discarded
// - Nat.half(8) == 4 
// - Nat.half(5) == 2 

Nat.half(a: Nat): Nat
  case a {
    zero: Nat.zero
    succ: case a.pred {
      zero: Nat.zero
      succ: Nat.succ(Nat.half(a.pred.pred))
    }
  }
