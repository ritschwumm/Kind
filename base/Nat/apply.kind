// Applies function (f) "a" times to "b"
// - Nat.apply!(2, Nat.double, 5) == 20
// - Nat.apply!(3, Bool.not, true) == false

Nat.apply<A: Type>(a: Nat, f: A -> A, b: A): A
  case a {
    zero: b
    succ: Nat.apply<A>(a.pred, f, f(b))
  }
